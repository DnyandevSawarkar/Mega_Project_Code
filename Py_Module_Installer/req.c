#include <stdlib.h>

int main() {
    // Install requests library
    system("pip install requests==2.31.0");

    // Install Flask library
    system("pip install Flask==3.0.1");

    // Add more pip install commands for other libraries if needed

    return 0;
}
